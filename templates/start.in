#!/bin/bash
INSTANCE=${buildout:directory}/bin/instance
BUILDOUT=${buildout:directory}/bin/buildout
export HOST=$URL
export SITE_ID=Plone
$INSTANCE -O$SITE_ID run set_virtual_hosting.py && $INSTANCE console || $BUILDOUT install demosite && $INSTANCE console
